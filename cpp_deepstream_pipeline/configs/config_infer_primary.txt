[property]
gpu-id=0
# 1. Paths to our ONNX model and the Engine it will build
onnx-file=../models/scrfd_10g_bnkps.onnx
model-engine-file=../models/scrfd_10g_bnkps.onnx_b3_gpu0_fp16.engine
labelfile-path=labels_primary.txt

# 2. Network Configuration
# 0=Detector, 1=Classifier, 2=Segmentation
network-type=0
# 0=FP32, 1=INT8, 2=FP16
network-mode=2
# Must match the batch-size in our master config (3 active cameras = 3)
batch-size=3
num-detected-classes=1 
force-implicit-batch-dim=0
gie-unique-id=1


# Normalization parameters (InsightFace standard: Mean 127.5, Std 128)
net-scale-factor=0.0078125
offsets=127.5;127.5;127.5

# 3. Model Architecture Details
# 0=RGB, 1=BGR, 2=GRAY
model-color-format=0
maintain-aspect-ratio=1
scaling-filter=0

# 4. SCRFD Custom Parsing
# DeepStream uses a custom C++ library to read SCRFD ONNX outputs.
parse-bbox-func-name=NvDsInferParseCustomSCRFD
custom-lib-path=../custom_parser/libnvdsinfer_custom_impl_scrfd.so

[class-attrs-all]
# Detection thresholds
pre-cluster-threshold=0.4
topk=20
nms-iou-threshold=0.45

